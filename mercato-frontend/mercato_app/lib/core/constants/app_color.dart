import 'package:flutter/material.dart';

abstract class AppColors {
  
  static const Color primary = Color(0xFF00E676);      // Vert vif de la maquette
  static const Color primaryDark = Color(0xFF00B248);  
  

  static const Color darkBackground = Color(0xFF0B141B); // Noir/Bleu très foncé de la maquette
  static const Color darkSurface = Color(0xFF16222F);    // Cartes et éléments interactifs
  static const Color darkInput = Color(0xFF1E2D3D);      // Champs de saisie
  

  static const Color lightBackground = Color(0xFFF5F7FA);
  static const Color lightSurface = Color(0xFFFFFFFF);
  
 
  static const Color textPrimaryDark = Color(0xFFFFFFFF);
  static const Color textSecondaryDark = Color(0xFF8C9BA5);
  static const Color textPrimaryLight = Color(0xFF1A1A1A);
  static const Color textSecondaryLight = Color(0xFF757575);
}